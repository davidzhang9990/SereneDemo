namespace BowenSerene.Default {
    export enum PurchaseShareType {
        VolumeShare = 1,
        WeightShare = 2
    }
    Serenity.Decorators.registerEnum(PurchaseShareType, 'Purchase.ShareType');
}

